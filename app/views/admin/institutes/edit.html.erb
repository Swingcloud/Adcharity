<h2>新增基金會</h2>
<%= form_for [:admin, @institute] do |f| %>
	<% if @institute.errors.any? %>
    <ul>
      <% @institute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
	<% end %>
	<%= render :partial => "form", :locals => {:f => f} %>
	<%= f.submit "送出" , :class =>"btn btn-primary"%>
<% end %>