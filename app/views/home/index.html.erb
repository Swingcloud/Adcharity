<h1 class="text-center">AdCharity</h1>
<div class="container">
	<div>
		<ul>
			<% @projects.each do |p|%>
				<li><%= p.name%></li>
				<li><%= p.description%></li>
				<li><%= p.donate_amount %></li>
			<% end %>
			</ul>
	</div>
	<div class="embed-container"> 
		<%= embed(Advertisement.first.ad_href) %> 
	</div>
</div>
