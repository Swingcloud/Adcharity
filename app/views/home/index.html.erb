<h1 class="text-center">AdCharity</h1>
<div class="container">
	<div class="row">
		<% @projects.each do |p|%>
			<div class="col-sm-4">
				<ul>
					<li><%= p.name %></li>
					<li><%= p.description %></li>
					<li><%= p.donate_amount %></li>
					<li><%= link_to'詳細專案', project_path(p) %>	</li>
			</ul>
			</div>
		<% end %>
	</div>
</div>
